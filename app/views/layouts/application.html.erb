<!DOCTYPE html>
<html>
<head>
  <title>MyPosts</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true, type: "module" %>
</head>

<body class="bg-gray-50 text-gray-900">
  <nav class="bg-gradient-to-r from-indigo-500 to-purple-600 p-4 text-white shadow-lg rounded-b-lg">
    <div class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-2xl font-semibold tracking-wide">MyApp</a>

      <div class="space-x-6">
        <%= link_to "Posts", "/", class: "hover:underline text-lg transition duration-300 hover:text-gray-200" %>
        <%= link_to "Users", "/", class: "hover:underline text-lg transition duration-300 hover:text-gray-200" %>
      </div>
    </div>
  </nav>

  <main class="container mx-auto p-6">
    <div class="bg-white shadow-md rounded-lg p-6">
      <%= yield %>
    </div>
  </main>

  <footer class="bg-gray-900 text-gray-400 text-center p-4 mt-8 rounded-t-lg">
    &copy; <%= Time.current.year %> MyPosts. All rights reserved.
  </footer>
</body>
</html>
